# config-driven-ui-builder
A React application that dynamically generates complete user interfaces from JSON configuration schemas. Features a recursive rendering engine, component registry pattern, conditional rendering (showIf/hideIf with AND/OR), validation engine, nested layouts, and light/dark theme support — zero hardcoded UI.
